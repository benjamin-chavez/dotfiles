// ./.dotfiles/qmk/keychron_k2_he/config.h

// Optional QMK configuration overrides.
// These are commented out because the defaults work well for most users.
// Uncomment and adjust if the Caps Lock tap/hold behavior feels off.
//
// Docs: https://docs.qmk.fm/tap_hold

#pragma once

// // TAPPING_TERM: Time in ms to distinguish tap from hold (default: 200ms)
// // - Lower value: faster tap detection, but easier to accidentally trigger hold
// // - Higher value: more forgiving taps, but slower hold activation
// #define TAPPING_TERM 200

// // PERMISSIVE_HOLD: Makes hold trigger more reliably during fast typing
// // Without this: tap Caps, then quickly tap another key = both are taps
// // With this: tap Caps, then quickly tap another key = Caps is hold + key
// #define PERMISSIVE_HOLD